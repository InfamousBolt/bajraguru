{
  "name": "bajraguru",
  "version": "1.0.0",
  "private": true,
  "main": "server/src/index.js",
  "scripts": {
    "build": "cd client && npm install --include=dev && npm run build",
    "postinstall": "cd server && npm install && npm rebuild better-sqlite3",
    "start": "node server/src/index.js"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
